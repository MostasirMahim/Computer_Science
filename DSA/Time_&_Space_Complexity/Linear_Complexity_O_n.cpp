#include<bits/stdc++.h>
using namespace std;

int main() {
    int n;//O(1)
    cin >> n; //O(1)

    for (int i=1;i<=n;i++) { //O(n)
        cout << i << " ";
    }
        //OUTPUT : 1 2 3 4 5
        //So Depends on n & Its worst case so its final complexity
    return 0; //O(1)
}